[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "xr-teleoperate-g1d"
version = "0.1.0"
description = "G1-D XR teleoperation mapping, motion bridge and collection schema"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["xr_teleoperate_g1d*"]
